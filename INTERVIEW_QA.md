# 面試 Q&A — Stock Assistant

## Q1：你怎麼定義「值得關注」？為什麼不是「值得買」？

技術指標只能看出目前的動能與結構，不能預測未來。所以我把任務定義成「篩選」而不是「決策」——系統負責從市場裡找出訊號集中的股票，但買不買還是人判斷。這樣設計也讓 LLM 的角色更清楚：它是在解釋系統算出來的數字，不是在替你做投資決定。

---

## Q2：為什麼選這 5 個指標？有沒有考慮過 MACD 或 KD？

我用 AI 協助篩選候選指標，但自己做了驗收——確認每個指標測量的面向不重疊，MA 看趨勢方向、Volume 看資金動能、RSI 看超買超賣、Stability 看風險，四個維度互不重複。MACD 和 KD 是 MA 的衍生指標，加進來是重複計分，所以排除了。

---

## Q3：你怎麼用 LLM？

我把 LLM 和規則層分開。所有計算都是 Python 確定性地算出來的，LLM 收到的不是原始股價資料，而是結構化摘要：五個指標各是多少分、總分幾分、結論是什麼。它的工作是把數字翻譯成兩種受眾能讀懂的語言——散戶看 narrative，法人看數字明細。就算 LLM 幻覺，範圍也被框住了，不能自己加料，量化評分那層永遠是可信的。

---

## Q4：如果 LLM 回傳格式壞掉怎麼辦？

streaming 結束後才嘗試解析 JSON，parse 失敗就 fallback 顯示原始文字。量化評分本來就不依賴 LLM，所以就算 LLM 完全炸掉，圖表和分數還是正常的。

---

## Q5：TWSE API 你怎麼處理？遇到什麼問題？

開發中發現原本的 endpoint 已被下架，改用新 endpoint 後格式也變了，解析邏輯和測試資料都要一起更新。穩定性的部分做了指數退避 retry 三次，針對股票不存在、API 無法存取、資料不足三種情境分別回傳對應錯誤，讓前端顯示有意義的說明。

---

## Q6：你怎麼確保別人能跑起來？

三層：README 說明啟動方式；.env.example 列出需要的 key；Docker 打包讓任何有 Docker 的環境一行指令就能啟動。核心邏輯有 29 個單元測試覆蓋指標計算和邊界條件。

---

## Q7：你說這是 RAG，但標準 RAG 通常用向量資料庫做語意搜尋。你只用關鍵字查 RSS，這樣算 RAG 嗎？

這是一個簡化版的 RAG pattern。完整的 RAG 會做 embedding → vector search → rerank，但那需要建資料庫、維護 index。我的 Retrieve 步驟改用 Google News RSS 關鍵字搜尋——它本質上也是「根據查詢詞從外部知識庫取回相關文件」，只是搜尋機制不同。對這個專案來說，新聞本來就是按股票代號和名稱分類的，關鍵字搜尋的精準度已經夠用，不需要語意搜尋來消歧義。核心的 Augment → Generate 流程是完整的。

---

## Q8：你只把新聞標題餵給 LLM，沒有全文。這樣能做出有意義的分析嗎？

只有標題確實是限制，但這是刻意的設計取捨。抓全文有幾個問題：Google News 的連結是 redirect、各新聞網站有付費牆、爬內文會有法律灰色地帶，而且全文進 prompt 會大幅增加 token 用量和延遲。標題本身已經包含事件摘要——「無預警停牌」、「配息145元加配股票」這種標題已經有足夠的信號讓 LLM 做方向性判斷。它不是在分析全文，而是在把新聞事件的性質（正面/負面/風險）納入技術分析的脈絡。這和分析師看新聞 headline 做初步判斷是一樣的邏輯。

---

## Q9：Google News RSS 不穩定，你怎麼確保這不影響使用者體驗？

新聞抓取是獨立的非必要路徑。`fetch_recent_news()` 任何例外都 catch 住、回傳空 list，不拋錯。LLM 收到空 list 就跳過新聞區塊，照常只用技術指標生成分析。使用者看到的最壞情況是「沒有參考新聞來源 expander」，核心的圖表和評分完全不受影響。這是 graceful degradation——把可靠性高的部分（本地計算）和不穩定的外部服務（RSS）分層，後者失敗不會拖垮前者。

---

## Q10：你怎麼確保 LLM 引用新聞時不會過度推論或產生幻覺？

我無法 100% 保證 LLM 不會在引用新聞時過度推論——這是 LLM 的固有限制。但有幾層設計縮小風險：第一，system prompt 明確說「只能根據給定的量化指標和提供的新聞標題進行說明」；第二，UI 上有「參考新聞來源」expander，使用者可以直接點進去驗證標題內容；第三，量化評分那層完全不依賴 LLM，就算 LLM 說錯了，數字還在那裡。我把 LLM 的角色框定在「解釋與說明」，不讓它做計算或決策，這樣即使有幻覺，影響範圍也被限制在文字說明裡。

---

## Q11：你怎麼評估這個 RAG 功能的效果？有沒有辦法量化它讓分析變好了多少？

坦白說，這個版本沒有做定量評估，這是明確的限制。要量化 RAG 的效果，標準做法是準備一批測試股票、讓有/無新聞兩個版本各跑一次，再由人工評分 summary 的相關性和準確度——這需要時間和標注資源，超出了這個專案的範圍。但從實際輸出來看，LLM 在有新聞的情況下會把標題裡的事件（例如「停牌」、「配息」）納入風險或訊號說明，沒有新聞時則只有技術面描述。這是可觀察的行為差異。如果要做正式評估，我會用 RAGAS 這類框架衡量 faithfulness 和 answer relevancy。

---

## Q12：這個 RAG 功能如果要升級成 production-ready，你會怎麼改？

有幾個方向，按優先順序：第一，把新聞全文爬下來做 chunking + embedding，存進向量資料庫（例如 pgvector 或 Chroma），讓 Retrieve 步驟從關鍵字搜尋升級成語意搜尋，能找到標題沒有股票代號但內容相關的新聞。第二，加 reranker——先多抓幾篇，再用輕量模型挑出最相關的 top-k 給 LLM，避免無關新聞干擾。第三，加 citation verification——讓 LLM 輸出引用的新聞索引號，驗證它引用的內容確實在提供的來源裡，降低幻覺風險。但這些對這個作業來說是 over-engineering，現在的版本已經完整展示了 Retrieve → Augment → Generate 的核心 pattern，這才是重點。

---

## 補充說明（備用）

### 散戶／法人 UI 設計動機

我在設計輸出格式之前，跟一個有在投資的朋友討論過。他是財金背景，看股票的方式跟一般人不一樣——他直接看數字，不需要 AI 解釋。這讓我意識到同一個系統，不同人要的東西差很多，所以我設計了兩種輸出模式。

> 如果被追問「他具體說了什麼」：他沒有很明確地說，但跟他聊完我發現他問的問題都是「RSI 多少」、「量有沒有放大」，不是「這支股票好不好」。這讓我決定做法人模式，直接給數字。
